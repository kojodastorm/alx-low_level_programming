#include <stdio.h>

/**
 * main - prints the name of the program
 *
 * @argc: argument count
 * @argv: argument vector
 *
 * Return: void
 */

int main(int argc, char *argv[])
{
	if (argv)
	{
		printf("%d\n", argc);
	}
	return (0);
}
